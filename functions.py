# str() is a built-in function in Python that converts any data type into a string.
# int() is a built-in function in python that converts any data type into an integer.
# f-strings (formatted string literals) are a modern and clean way to build strings that include variables.
